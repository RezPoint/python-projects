# Словарь (dictionary) — это коллекция пар "ключ: значение".
# Ключи уникальны, а значения могут повторяться.

# Пример простого словаря:
user = {
    "name": "RezPoin",
    "age": 19,
    "skills": ["Python", "GitHub", "Data Science"]
}

# 🔹 Обращаемся к элементам по ключу:
print("Имя:", user["name"])
print("Возраст:", user["age"])

# 🔹 Добавляем новый элемент:
user["city"] = "Москва"

# 🔹 Меняем значение:
user["age"] = 21

# 🔹 Удаляем элемент:
del user["city"]

# 🔹 Перебираем словарь:
for key, value in user.items():
    print(f"{key}: {value}")

# 🔹 Проверяем наличие ключа:
if "skills" in user:
    print("Навыки:", user["skills"])

# 🔹 Получаем только ключи или значения:
print("Ключи:", list(user.keys()))
print("Значения:", list(user.values()))

# 🔹 Безопасный доступ (если ключа нет — не будет ошибки):
print("Город:", user.get("city", "Не указан"))